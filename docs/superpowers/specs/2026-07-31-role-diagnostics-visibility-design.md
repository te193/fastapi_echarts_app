# 角色诊断区域展开规则设计

## 目标

让角色诊断区域的展开状态始终跟随当前分析大类。

## 行为

- 页面进入时，当前大类为“销售角色”（ID 1）则展开。
- 当前大类切换为其他分类时立即折叠。
- 再次选择“销售角色”时重新展开。
- 诊断子标签或历史筛选条件不再决定该区域是否展开。
- 用户仍可在“销售角色”大类下手动折叠；下次重新切回销售角色时再次展开。

## 约束

- 仅修改前端展开状态，不改变接口、筛选条件、诊断查询和业务逻辑。
- 验证在后台无界面环境执行，不操作用户前台浏览器。
