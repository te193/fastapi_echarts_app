# 调价角色双层时间轴筛选实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将角色与定价迁移页筛选区实现为已确认的方案 C 双层时间轴工作台，同时保持全部现有筛选参数和数据行为不变。

**Architecture:** 保留现有表单元素 ID 和 `price_review_role.js` 状态流，只在 Jinja 模板中增加语义包裹层，并在全局样式中为角色模式提供定向布局。桌面端采用“日期 / 时间轴 / 状态”上层与“范围筛选 / 操作”下层，响应式断点下按阅读顺序重排。

**Tech Stack:** FastAPI、Jinja2、原生 JavaScript、CSS、pytest、Playwright CLI。

## Global Constraints

- 不修改角色计算规则、接口字段、数据库、ETL 或历史范围。
- 保留现有元素 ID、查询参数、筛选状态保存、高级筛选和重置逻辑。
- 不重做经营效果筛选区，不新增前端依赖。
- 自动化测试在隐藏后台进程运行；浏览器验收使用无头模式。
- 视觉沿用现有看板的字体、色板、圆角、边框和阴影体系。

---

### Task 1: 用前端结构测试锁定双层工作台

**Files:**
- Modify: `tests/test_price_review_frontend.py`
- Test: `tests/test_price_review_frontend.py`

**Interfaces:**
- Consumes: `app/templates/price_review.html` 与 `app/static/css/styles.css` 的静态内容。
- Produces: 对 `role-filter-workbench`、`role-filter-timeline`、`role-filter-scope-bar` 和响应式规则的回归约束。

- [ ] **Step 1: 写失败测试**

```python
def test_station_role_filters_use_two_tier_timeline_workbench():
    template = (ROOT / "app" / "templates" / "price_review.html").read_text(encoding="utf-8")
    styles = (ROOT / "app" / "static" / "css" / "styles.css").read_text(encoding="utf-8")
    assert 'class="role-filter-workbench"' in template
    assert 'class="role-filter-timeline"' in template
    assert 'class="role-filter-scope-bar"' in template
    assert ".role-filter-workbench" in styles
    assert ".role-filter-timeline" in styles
    assert ".role-filter-scope-bar" in styles
```

- [ ] **Step 2: 后台运行测试并确认因新结构缺失而失败**

Run: `pytest tests/test_price_review_frontend.py -q`

Expected: FAIL，缺少 `role-filter-workbench`。

- [ ] **Step 3: 不修改生产代码，记录失败结果后进入 Task 2**

### Task 2: 实现双层时间轴筛选区

**Files:**
- Modify: `app/templates/price_review.html:63-156`
- Modify: `app/static/css/styles.css:3332-3850`
- Modify: `app/templates/base.html`（仅静态资源版本号，如缓存验证需要）
- Modify: `tests/test_price_review_frontend.py`

**Interfaces:**
- Consumes: `priceReviewRoleFilters`、`roleAdjustDateSelect`、`rolePreDaysSelect`、`rolePostDaysSelect`、`roleCountrySelect`、`roleStoreSelect`、`roleKeywordInput` 及现有按钮 ID。
- Produces: 两层筛选容器；JavaScript 无需改变选择器或请求参数。

- [ ] **Step 1: 调整模板结构但保留所有 ID**

```html
<div class="role-filter-workbench">
  <div class="role-filter-timeline-row">
    <label class="role-filter-date-block"><span>本次调价批次</span><select id="roleAdjustDateSelect"></select></label>
    <div class="role-filter-timeline"><select id="rolePreDaysSelect"></select><span>调价日</span><select id="rolePostDaysSelect"></select></div>
    <aside class="role-filter-data-status"><span id="roleCacheStatusMeta">角色快照 —</span></aside>
  </div>
  <div class="role-filter-scope-bar"><select id="roleCountrySelect"></select><select id="roleStoreSelect"></select><input id="roleKeywordInput"><button id="applyRoleFiltersBtn">应用筛选</button></div>
</div>
```

- [ ] **Step 2: 实现连续外层、时间轴和低权重筛选带样式**

```css
.role-filter-workbench { overflow: hidden; border: 1px solid var(--line); border-radius: 18px; }
.role-filter-timeline-row { display: grid; grid-template-columns: 210px minmax(420px, 1fr) 220px; }
.role-filter-scope-bar { display: grid; grid-template-columns: minmax(150px, .8fr) minmax(150px, .8fr) minmax(220px, 1.3fr) auto; }
```

- [ ] **Step 3: 增加 1280px、900px 响应式重排**

在 1280px 下将状态移到第二行；在 900px 下按日期、调前、调价日、调后的顺序纵向排列，并让范围筛选改为两列/单列，禁止页面横向溢出。

- [ ] **Step 4: 后台运行前端测试确认通过**

Run: `pytest tests/test_price_review_frontend.py -q`

Expected: PASS。

- [ ] **Step 5: 检查 JavaScript 语法**

Run: `node --check app/static/js/price_review_role.js`

Expected: exit code 0。

### Task 3: 视觉和交互验收

**Files:**
- Verify: `app/templates/price_review.html`
- Verify: `app/static/css/styles.css`
- Verify: `app/static/js/price_review_role.js`

**Interfaces:**
- Consumes: 8001 测试服务与真实角色迁移接口响应。
- Produces: 桌面、窄屏无头浏览器验收结果，不保留临时截图和日志。

- [ ] **Step 1: 隐藏后台启动或复用 8001 测试服务**

确认服务进程 `MainWindowHandle=0`，访问 `/price-review` 返回 200。

- [ ] **Step 2: 桌面端验收**

在 1720×950 下切换到“角色与定价迁移”，检查双层结构、时间轴、状态区、高级筛选和主按钮层级，并确认页面无横向溢出。

- [ ] **Step 3: 窄屏验收**

在 900×900 下检查阅读顺序、控件重排、文本不重叠和无页面横向溢出。

- [ ] **Step 4: 运行完整测试**

Run: `pytest -q`

Expected: 全部通过。

- [ ] **Step 5: 清理临时浏览器会话、截图和运行日志**

仅删除本次验收生成且已确认位于工作区内的 `.playwright-cli`、`.codex-runtime` 临时文件和视觉伴侣会话，不删除用户已有输出。

- [ ] **Step 6: 提交相关代码**

仅暂存本次双层时间轴模板、样式、测试及必要的静态版本号改动，提交信息使用中文。
