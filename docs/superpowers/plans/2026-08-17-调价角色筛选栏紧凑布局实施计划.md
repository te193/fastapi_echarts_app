# 调价角色筛选栏紧凑布局实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将角色迁移页的高频角色筛选提升到第一排，缩短基础筛选宽度，并把展开入口改名为“更多筛选”。

**Architecture:** 保留现有表单元素 ID 和 JavaScript 状态模型，仅重排 Jinja 模板中的控件归属，并为角色模式增加专用桌面与响应式网格规则。测试通过模板结构和 CSS 约束防止高频条件重新落回展开区或产生横向溢出。

**Tech Stack:** Jinja2、原生 CSS、原生 JavaScript、pytest、Playwright CLI。

## Global Constraints

- 不修改角色筛选接口、请求参数、元素 ID 和 JavaScript 状态语义。
- 第一排固定显示国家、店铺、关键词、调前角色、调后角色、角色变化。
- 展开区只保留财务区间变化和数据状态。
- 展开入口文案固定为“更多筛选”。
- 自动化测试与无头浏览器验收必须后台隐藏运行。

---

### Task 1: 重排并压缩角色筛选栏

**Files:**
- Modify: `app/templates/price_review.html:141-157`
- Modify: `app/static/css/styles.css:3700-3817,4212-4301`
- Modify: `app/templates/base.html:10`
- Modify: `app/templates/price_review.html:398-399`
- Test: `tests/test_price_review_frontend.py`
- Test: `tests/test_replenishment_frontend.py`

**Interfaces:**
- Consumes: `price_review_role.js` 通过 `roleCountrySelect`、`roleStoreSelect`、`roleKeywordInput`、`roleBeforeSelect`、`roleAfterSelect`、`roleChangeSelect`、`financeChangeSelect`、`roleDataStatusSelect` 读写筛选状态。
- Produces: 相同 ID 的表单控件和 `roleAdvancedFilters` 展开容器；JavaScript 无需修改。

- [ ] **Step 1: 写入失败的结构和样式测试**

```python
def test_station_role_primary_filters_promote_role_controls_and_use_more_filters_copy():
    template = (ROOT / "app" / "templates" / "price_review.html").read_text(encoding="utf-8")
    scope = template.split('<div class="role-filter-scope-bar">', 1)[1].split('</div>', 1)[0]
    advanced = template.split('<div id="roleAdvancedFilters"', 1)[1].split('</div>', 1)[0]

    for control_id in ("roleBeforeSelect", "roleAfterSelect", "roleChangeSelect"):
        assert f'id="{control_id}"' in scope
        assert f'id="{control_id}"' not in advanced
    for control_id in ("financeChangeSelect", "roleDataStatusSelect"):
        assert f'id="{control_id}"' in advanced
    assert "更多筛选" in scope
    assert "高级筛选" not in scope


def test_station_role_primary_filter_grid_is_compact_and_responsive():
    styles = (ROOT / "app" / "static" / "css" / "styles.css").read_text(encoding="utf-8")
    assert "role-filter-role" in styles
    assert "grid-template-columns: minmax(120px, 0.65fr)" in styles
    assert "grid-template-columns: repeat(2, minmax(150px, 1fr));" in styles
```

- [ ] **Step 2: 后台运行定向测试并确认因结构尚未改变而失败**

Run: `python -m pytest tests/test_price_review_frontend.py -q`

Expected: FAIL，失败点为第一排缺少 `roleBeforeSelect`、`roleAfterSelect`、`roleChangeSelect` 或仍包含“高级筛选”。

- [ ] **Step 3: 最小化调整模板和 CSS**

将调前角色、调后角色、角色变化三个现有 `<label>` 移入 `role-filter-scope-bar`，添加 `role-filter-role` 类；展开区只留下财务变化和数据状态。把按钮文案改为“更多筛选”。桌面网格采用紧凑基础列、三列角色列和自适应操作列；1280px 以下降级为三列，900px 以下降级为两列，620px 以下为单列。展开区桌面固定为两列。

- [ ] **Step 4: 更新静态资源缓存版本**

把 `styles.css`、`price_review.js` 和 `price_review_role.js` 的查询版本统一更新为本次布局版本，并同步修改对应缓存版本测试。

- [ ] **Step 5: 后台运行定向测试并确认通过**

Run: `python -m pytest tests/test_price_review_frontend.py tests/test_replenishment_frontend.py -q`

Expected: PASS，零失败。

- [ ] **Step 6: 使用无头浏览器验收桌面和窄屏**

在 1720×950 和 900×900 下打开 `/price-review`，切换到“角色与定价迁移”，分别验证常显筛选顺序、更多筛选展开内容、按钮内收以及 `document.documentElement.scrollWidth === document.documentElement.clientWidth`。

- [ ] **Step 7: 后台运行完整回归测试**

Run: `python -m pytest -q`

Expected: 全部测试通过，零失败。

- [ ] **Step 8: 提交实现**

```bash
git add app/templates/price_review.html app/templates/base.html app/static/css/styles.css tests/test_price_review_frontend.py tests/test_replenishment_frontend.py
git commit -m "调价复盘：紧凑角色筛选栏布局"
```
