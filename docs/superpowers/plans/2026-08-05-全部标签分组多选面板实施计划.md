# 全部标签分组多选面板实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将经营明细筛选区的“全部标签”扁平 SlimSelect 改造成保留搜索、按大类连续分组、支持跨组多选的自定义复选面板。

**Architecture:** 保留 `labelHubDetailLabels` 原生多选框作为筛选状态和接口参数的兼容层，在其外部新增专用触发框与分组面板。自定义复选框只同步原生选项的 `selected` 状态，现有 `collectDetailFilters()`、`serializeDetailConditions()`、Chip 删除、重置和 `/api/label-hub/details` 参数链路保持不变。

**Tech Stack:** 原生 JavaScript、Jinja2 HTML、CSS、pytest、Playwright CLI。

## Global Constraints

- 不修改后端接口、筛选参数、条件值格式和业务查询逻辑。
- 原生选择值继续使用 `父标签ID:子标签ID`。
- 勾选标签后不立即请求数据，继续由现有“应用筛选”按钮统一提交。
- 搜索支持大类名和子标签名；搜索不得清除被隐藏的已选项。
- 桌面端子标签两列，窄屏单列，面板内部滚动。
- 不增加大类全选、反选、最近使用等额外功能。
- 只改造“全部标签”，其他 SlimSelect 控件保持原样。
- 浏览器验证使用独立后台端口，不影响用户当前页面。

---

### Task 1: 构建全部标签专用分组面板

**Files:**
- Modify: `app/templates/label_hub.html:168`
- Modify: `app/static/js/label_hub.js:80-180, 470-486, 614-635, 1013-1058, 1071-1125`
- Modify: `app/static/css/styles.css:16310-16421` 附近
- Test: `tests/test_label_hub_frontend.py`

**Interfaces:**
- Consumes: `meta.categories`、`meta.excluded_categories`、`detailState.detail_conditions`、`detailConditionValues()`、`selectedValues(select)`。
- Produces: `detailLabelGroups()`、`renderDetailLabelPanel(searchText)`、`updateDetailLabelTrigger()`、`toggleDetailLabelPanel()`、`closeDetailLabelPanel()`、`syncDetailLabelControl()`。
- Compatibility: `labelHubDetailLabels` 继续供 `collectDetailFilters()`、`renderDetailActiveFilters()` 和 `serializeDetailConditions()` 使用。

- [x] **Step 1: 写入失败的前端结构与行为契约测试**

在 `tests/test_label_hub_frontend.py` 新增测试，约束专用控件结构、函数和兼容链路：

```python
def test_detail_labels_use_searchable_grouped_checkbox_panel():
    template = (ROOT / "app" / "templates" / "label_hub.html").read_text(encoding="utf-8")
    script = (ROOT / "app" / "static" / "js" / "label_hub.js").read_text(encoding="utf-8")
    styles = (ROOT / "app" / "static" / "css" / "styles.css").read_text(encoding="utf-8")

    for element_id in (
        "labelHubDetailLabelTrigger",
        "labelHubDetailLabelSummary",
        "labelHubDetailLabelPanel",
        "labelHubDetailLabelSearch",
        "labelHubDetailLabelGroups",
        "labelHubDetailLabelEmpty",
    ):
        assert f'id="{element_id}"' in template
    assert 'id="labelHubDetailLabels" multiple hidden' in template
    for function_name in (
        "detailLabelGroups",
        "renderDetailLabelPanel",
        "updateDetailLabelTrigger",
        "toggleDetailLabelPanel",
        "closeDetailLabelPanel",
        "syncDetailLabelControl",
    ):
        assert f"function {function_name}" in script
    assert "data-detail-label-value" in script
    assert "未找到匹配标签" in template
    assert "暂无可用标签" in script
    assert ".label-hub-detail-label-panel" in styles
    assert ".label-hub-detail-label-options" in styles
    assert "grid-template-columns: repeat(2, minmax(0, 1fr));" in styles

    detail_selects = script.split("function detailFilterSelects()", 1)[1].split(
        "function destroyDetailFilterSelects()", 1
    )[0]
    assert "labelHubDetailLabels" not in detail_selects
    assert "detailState.detail_conditions = serializeDetailConditions(selectedValues(elements.labelHubDetailLabels));" in script
```

- [x] **Step 2: 运行聚焦测试并确认 RED**

Run:

```powershell
pytest tests/test_label_hub_frontend.py -k "detail_labels_use_searchable_grouped_checkbox_panel" -q
```

Expected: FAIL，缺少 `labelHubDetailLabelTrigger` 等自定义面板结构。

- [x] **Step 3: 在模板中替换全部标签可视控件**

将原来的单行 `<label>` 改为专用控件，保留隐藏原生多选框：

```html
<div class="label-hub-detail-filter-control label-hub-detail-label-control">
  <span>标签</span>
  <button id="labelHubDetailLabelTrigger" class="label-hub-detail-label-trigger" type="button"
          aria-haspopup="true" aria-expanded="false" aria-controls="labelHubDetailLabelPanel">
    <span id="labelHubDetailLabelSummary">全部标签</span><i aria-hidden="true"></i>
  </button>
  <div id="labelHubDetailLabelPanel" class="label-hub-detail-label-panel" hidden>
    <div class="label-hub-detail-label-search-wrap">
      <input id="labelHubDetailLabelSearch" type="search" placeholder="搜索标签名称" autocomplete="off">
    </div>
    <div class="label-hub-detail-label-panel-head">
      <strong>全部标签 · MSKU维度</strong><span>支持跨分类多选</span>
    </div>
    <div id="labelHubDetailLabelGroups" class="label-hub-detail-label-groups"></div>
    <p id="labelHubDetailLabelEmpty" class="label-hub-detail-label-empty" hidden>未找到匹配标签</p>
  </div>
  <select id="labelHubDetailLabels" multiple hidden data-placeholder="全部标签" aria-label="明细标签多选"></select>
</div>
```

- [x] **Step 4: 添加元素缓存与事件绑定**

把以下 ID 加入 `cacheElements()` 的元素列表：

```javascript
"labelHubDetailLabelTrigger", "labelHubDetailLabelSummary", "labelHubDetailLabelPanel",
"labelHubDetailLabelSearch", "labelHubDetailLabelGroups", "labelHubDetailLabelEmpty"
```

在 `bindEvents()` 中添加：

```javascript
elements.labelHubDetailLabelTrigger.addEventListener("click", function (event) {
  event.stopPropagation();
  toggleDetailLabelPanel();
});
elements.labelHubDetailLabelSearch.addEventListener("input", function () {
  renderDetailLabelPanel(this.value);
});
elements.labelHubDetailLabelGroups.addEventListener("change", function (event) {
  var checkbox = event.target.closest("[data-detail-label-value]");
  if (!checkbox) return;
  var option = Array.from(elements.labelHubDetailLabels.options).find(function (item) {
    return String(item.value) === String(checkbox.dataset.detailLabelValue);
  });
  if (option) option.selected = checkbox.checked;
  updateDetailLabelTrigger();
});
```

在现有 document 点击与 `Escape` 处理逻辑中同时关闭标签面板，且不重复注册全局事件。

- [x] **Step 5: 实现分组数据与原生选项构建**

用保留父子结构的数据函数替代当前扁平逻辑：

```javascript
function detailLabelGroups() {
  return (meta.categories || []).concat(meta.excluded_categories || []).map(function (category) {
    return {
      id: Number(category.id),
      label: String(category.label || ""),
      children: (category.children || []).map(function (child) {
        return {
          value: category.id + ":" + child.id,
          label: String(child.label || "")
        };
      })
    };
  }).filter(function (group) { return group.children.length > 0; });
}

function detailLabelOptions() {
  return detailLabelGroups().reduce(function (all, group) {
    return all.concat(group.children.map(function (child) {
      return { key: child.value, label: group.label + " · " + child.label };
    }));
  }, []);
}
```

这确保隐藏原生选项、Chip 文案和 `父ID:子ID` 参数保持兼容。

- [x] **Step 6: 实现搜索、分组渲染和空状态**

`renderDetailLabelPanel(searchText)` 按以下规则生成 HTML：

```javascript
function renderDetailLabelPanel(searchText) {
  var query = String(searchText || "").trim().toLocaleLowerCase();
  var selected = {};
  selectedValues(elements.labelHubDetailLabels).forEach(function (value) { selected[String(value)] = true; });
  var html = [];
  detailLabelGroups().forEach(function (group) {
    var groupMatches = !query || group.label.toLocaleLowerCase().indexOf(query) >= 0;
    var children = group.children.filter(function (child) {
      return groupMatches || child.label.toLocaleLowerCase().indexOf(query) >= 0;
    });
    if (!children.length) return;
    html.push('<section class="label-hub-detail-label-section"><h4>' + app.escapeHtml(group.label) + '</h4>');
    html.push('<div class="label-hub-detail-label-options">');
    children.forEach(function (child) {
      html.push('<label class="label-hub-detail-label-option" title="' + app.escapeHtml(child.label) + '">');
      html.push('<input type="checkbox" data-detail-label-value="' + app.escapeHtml(child.value) + '"' +
        (selected[child.value] ? " checked" : "") + '><span>' + app.escapeHtml(child.label) + '</span></label>');
    });
    html.push("</div></section>");
  });
  elements.labelHubDetailLabelGroups.innerHTML = html.join("");
  elements.labelHubDetailLabelEmpty.textContent = detailLabelGroups().length ? "未找到匹配标签" : "暂无可用标签";
  elements.labelHubDetailLabelEmpty.hidden = html.length > 0;
}
```

搜索只重绘可见复选框；选择状态每次都从隐藏原生 `select` 获取，因此不会丢失搜索隐藏项。

- [x] **Step 7: 实现触发框、同步、开关和关闭**

实现摘要及面板生命周期：

```javascript
function updateDetailLabelTrigger() {
  var selected = selectedValues(elements.labelHubDetailLabels);
  var label = "全部标签";
  if (selected.length === 1) label = selectedOptionLabel(elements.labelHubDetailLabels, selected[0]).split(" · ").pop();
  if (selected.length > 1) label = "已选 " + selected.length + " 项";
  elements.labelHubDetailLabelSummary.textContent = label;
  elements.labelHubDetailLabelTrigger.classList.toggle("has-value", selected.length > 0);
}

function syncDetailLabelControl() {
  renderDetailLabelPanel("");
  elements.labelHubDetailLabelSearch.value = "";
  updateDetailLabelTrigger();
}

function closeDetailLabelPanel() {
  if (!elements.labelHubDetailLabelPanel || elements.labelHubDetailLabelPanel.hidden) return;
  elements.labelHubDetailLabelPanel.hidden = true;
  elements.labelHubDetailLabelTrigger.classList.remove("is-open");
  elements.labelHubDetailLabelTrigger.setAttribute("aria-expanded", "false");
  elements.labelHubDetailLabelSearch.value = "";
  renderDetailLabelPanel("");
}

function toggleDetailLabelPanel() {
  var willOpen = elements.labelHubDetailLabelPanel.hidden;
  if (!willOpen) return closeDetailLabelPanel();
  closeRoleReasonPanel();
  elements.labelHubDetailLabelPanel.hidden = false;
  elements.labelHubDetailLabelTrigger.classList.add("is-open");
  elements.labelHubDetailLabelTrigger.setAttribute("aria-expanded", "true");
  window.setTimeout(function () { elements.labelHubDetailLabelSearch.focus(); }, 0);
}
```

在 `populateControls()` 的原生选项与选择状态同步后调用 `syncDetailLabelControl()`；重置、Chip 删除和重新填充控件均沿用 `populateControls()` 进入同一同步路径。

- [x] **Step 8: 将全部标签移出 SlimSelect 初始化列表**

从 `detailFilterSelects()` 删除 `elements.labelHubDetailLabels`，保留国家类别、店铺、国家、销售角色、日销、毛利和排名等现有 SlimSelect。

这避免隐藏原生 `select` 被 SlimSelect 再次包装，同时不改变其他筛选控件。

- [x] **Step 9: 添加与角色原因一致的专用样式**

新增独立类，复用现有色彩与尺寸：

```css
.label-hub-detail-label-control { position: relative; }
.label-hub-detail-label-trigger {
  display: flex;
  align-items: center;
  justify-content: space-between;
  width: 100%;
  height: 36px;
  padding: 0 10px;
  border: 1px solid #c9d5e5;
  border-radius: 6px;
  background: #fff;
  color: #425b77;
  cursor: pointer;
}
.label-hub-detail-label-trigger.has-value { border-color: #9fc3ef; background: #f8fbff; color: #155ba6; }
.label-hub-detail-label-panel {
  position: absolute;
  z-index: 90;
  top: calc(100% + 6px);
  left: 0;
  width: min(440px, calc(100vw - 44px));
  max-height: min(480px, calc(100vh - 120px));
  overflow-y: auto;
  padding: 0 11px 10px;
  border: 1px solid #cfd9e7;
  border-radius: 8px;
  background: #fff;
  box-shadow: 0 12px 28px rgba(31, 58, 91, .17);
}
.label-hub-detail-label-panel[hidden] { display: none; }
.label-hub-detail-label-search-wrap { position: sticky; top: 0; z-index: 2; padding: 10px 0 7px; background: #fff; }
.label-hub-detail-label-search-wrap input { width: 100%; height: 32px; padding: 0 9px; border: 1px solid #d3deec; border-radius: 6px; }
.label-hub-detail-label-panel-head { display: flex; justify-content: space-between; gap: 12px; padding: 0 0 8px; color: #8190a3; font-size: 10px; }
.label-hub-detail-label-panel-head strong { color: #66788e; font-weight: 500; }
.label-hub-detail-label-section { padding: 8px 0; border-top: 1px solid #edf1f6; }
.label-hub-detail-label-section h4 { margin: 0 0 5px; color: #183d68; font-size: 12px; }
.label-hub-detail-label-options { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 4px 12px; }
.label-hub-detail-label-option { display: flex; align-items: center; min-width: 0; min-height: 24px; gap: 6px; color: #425b77; font-size: 11px; cursor: pointer; }
.label-hub-detail-label-option input { flex: 0 0 auto; width: 13px; height: 13px; margin: 0; accent-color: #1677e8; }
.label-hub-detail-label-option span { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.label-hub-detail-label-empty { margin: 14px 2px 4px; color: #8b99aa; font-size: 11px; text-align: center; }
@media (max-width: 720px) {
  .label-hub-detail-label-options { grid-template-columns: 1fr; }
}
```

触发框箭头直接复用角色原因触发框的伪元素尺寸与旋转规则，但使用 `.label-hub-detail-label-trigger` 独立选择器；不得修改角色原因面板类。

- [x] **Step 10: 更新静态资源版本并运行 GREEN 验证**

将 `app/templates/label_hub.html` 中 `label_hub.js` 的查询版本更新为 `?v=20260805labelgroups1`，并把 `tests/test_label_hub_frontend.py` 中对应的旧版本断言同步更新为该值。

Run:

```powershell
pytest tests/test_label_hub_frontend.py -k "detail_labels_use_searchable_grouped_checkbox_panel or role_reason or compact_toolbar" -q
pytest tests/test_label_hub_frontend.py -q
node --check app/static/js/label_hub.js
```

Expected: 所有命令退出码为 0。

- [x] **Step 11: 在独立浏览器中验证真实交互**

在非 8001/60485 的独立 Uvicorn 端口验证：

- 面板按大类连续分组，桌面端两列；
- 搜索大类显示该组全部标签；
- 搜索子标签只显示匹配项；
- 搜索无结果显示 `未找到匹配标签`；
- 跨大类勾选后触发框显示 `已选 N 项`；
- 清空搜索后此前选择仍保留；
- 点击“应用筛选”后已选条件 Chip 和明细查询同步变化；
- 点击外部和 `Esc` 可关闭；
- 重置可清空选择；
- 700px 视口下子标签为单列；
- 浏览器控制台无新增错误。

- [x] **Step 12: 全量验证并提交**

Run:

```powershell
pytest -q
node --check app/static/js/label_hub.js
git diff --check
git status --short
```

Expected: 全量测试通过、语法检查通过、差异检查无错误，仅包含本任务计划内文件。

Commit:

```powershell
git add app/templates/label_hub.html app/static/js/label_hub.js app/static/css/styles.css tests/test_label_hub_frontend.py docs/superpowers/plans/2026-08-05-全部标签分组多选面板实施计划.md
git commit -m "优化全部标签分组多选筛选"
```
